# Base URL for API calls (default uses Vite dev-server proxy in development)
VITE_API_BASE=/api/v1

# Dev-server proxy target — points Vite's /api proxy to the backend
VITE_API_PROXY=http://localhost:8000
